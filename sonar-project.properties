# Project identification
sonar.projectKey='CoopEducation'
sonar.projectName='CoopEducation'
sonar.projectVersion=1.0
sonar.token=sqp_9c572f494543b41c315d1af89cf35dee5642d32a

# Server connection
sonar.host.url=http://localhost:9000
# It's better to keep the token out of this file if you're using Git
# Pass the token via the command line for security

# Source configuration
sonar.sources=backend/src
sonar.sourceEncoding=UTF-8

# Exclusions (The fix for your current issue)
# Note: No leading slashes, and we use commas to separate patterns
sonar.exclusions=test/**/*, **/node_modules/**/*, coverage/**/*, dist/**/*, **/*.config.ts, **/*.d.ts, postcss.config.mjs, src/mock-data.ts

# Coverage reporting
sonar.javascript.lcov.reportPaths=coverage/lcov.info